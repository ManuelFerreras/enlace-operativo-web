import { OperationalSignature } from '@/components/operational-signature';
import type { Metadata } from 'next';
import { Button, SectionHeading, ServiceCard, StatCounter, CertificationBadge, TestimonialCard, Accordion, Marquee } from '@/components/ui';
import { ContactForm } from '@/components/contact-form';
import { LocationMap } from '@/components/location-map';
import { services, certifications, testimonials, faqs } from '@/content/site';
export const metadata:Metadata={title:'Sistema de diseño'};
export default function DesignSystem(){return <main id="main"><section className="section page-intro"><p className="eyebrow">Enlace Operativo / DESIGN SYSTEM 1.0</p><h1>Una misma lógica.<br/>En cada detalle.</h1><p>Perímetro invisible. Grafito, luz precisa y espacio para lo esencial.</p></section><section className="section ds-section"><SectionHeading number="01" label="FUNDAMENTOS">Color, tipo y ritmo.</SectionHeading><div className="swatches">{['background','surface','border','muted','foreground','accent'].map(color=><div key={color}><span className={`swatch swatch-${color}`}/><p className="mono">{color}</p></div>)}</div><div className="button-row"><Button href="/contacto">Acción principal</Button><Button variant="secondary" href="/servicios">Acción secundaria</Button><Button variant="ghost" href="/cobertura">Explorar</Button></div></section><section className="section ds-section"><SectionHeading number="02" label="INFORMACIÓN">Alcance concreto.</SectionHeading><div className="service-grid">{services.slice(0,2).map(service=><ServiceCard key={service.id} service={service}/>)}</div><div className="stats-row"><StatCounter value={6} label="Soluciones complementarias"/><StatCounter value={3} label="Sedes propuestas"/></div><div className="cert-grid">{certifications.map(cert=><CertificationBadge key={cert.name} {...cert}/>)}</div><TestimonialCard {...testimonials[0]}/></section><OperationalSignature/><Marquee/><section className="section ds-section"><SectionHeading number="03" label="INTERACCIÓN">Ubicar. Entender. Actuar.</SectionHeading><LocationMap/><Accordion items={faqs}/></section><section className="section ds-section"><SectionHeading number="04" label="FORMULARIO">Cada estado, previsto.</SectionHeading><ContactForm/></section></main>}
