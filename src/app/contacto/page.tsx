import type { Metadata } from 'next';
import { ContactForm } from '@/components/contact-form';
import { contact, services } from '@/content/site';
import { ArrowUpRight } from 'lucide-react';
export const metadata:Metadata={title:'Contacto',description:'Conversa con Enlace Operativo para diseñar un plan de seguridad a la medida.'};
export default async function Contact({searchParams}:{searchParams:Promise<{servicio?:string}>}){const query=await searchParams;const initialService=services.some(s=>s.slug===query.servicio)?query.servicio:'';return <main id="main"><section className="section page-intro"><p className="eyebrow">04 / CONTACTO</p><h1>Hablemos de lo<br/><span className="accent-text">que importa.</span></h1><p>Tu contexto es el punto de partida. Comparte las necesidades de tu operación o contáctanos directamente.</p></section><section className="section contact-page"><aside className="contact-details"><div><p className="eyebrow">CONVERSACIÓN DIRECTA</p><a href={contact.phoneHref}>{contact.phone}<ArrowUpRight size={20}/></a><a href="tel:+525591307754">{contact.secondaryPhone}<ArrowUpRight size={20}/></a><a className="button button-secondary" href={contact.whatsappHref} target="_blank" rel="noopener noreferrer">WhatsApp<ArrowUpRight size={18}/></a></div><div><p className="eyebrow">CORREO</p><a className="email-link" href={'mailto:'+contact.email}>{contact.email}</a></div><div><p className="eyebrow">OFICINA CENTRAL</p><p>{contact.address}</p><p>{contact.hours}</p></div><p className="microcopy">Contactos publicados en el dossier corporativo 2026. Las líneas indicadas son de atención comercial.</p></aside><ContactForm initialService={initialService}/></section></main>}
