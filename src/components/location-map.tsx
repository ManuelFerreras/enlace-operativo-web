'use client';
import { useState } from 'react';
import { ArrowUpRight } from 'lucide-react';
import Link from 'next/link';
import { locations } from '@/content/site';
export function LocationMap() { const [selected,setSelected]=useState(0);const location=locations[selected]; return <div className="coverage-layout"><div className="map-visual"><div className="map-grid"/><div className="map-loading-label mono">MÉXICO / COBERTURA PROPUESTA</div><div className="location-placeholder">{locations.map((place,index)=><button type="button" key={place.id} className={`location-pill ${selected===index?'active':''}`} onClick={()=>setSelected(index)} aria-pressed={selected===index}><span className="status-dot"/>{place.name}<span className="mono">{place.id}</span></button>)}</div><div className="map-legend mono"><span className="status-dot"/> SEDES POR VALIDAR</div></div><div className="location-detail"><div className="location-tabs" aria-label="Selecciona una sede">{locations.map((place,index)=><button key={place.id} onClick={()=>setSelected(index)} aria-pressed={selected===index}>{place.id}</button>)}</div><div aria-live="polite" aria-atomic="true"><p className="eyebrow">{location.region} / MÉXICO</p><h3>{location.name}</h3><p>{location.description}</p><p className="microcopy">{location.note}</p></div><Link className="button button-ghost" href="/contacto">Consultar disponibilidad<ArrowUpRight size={18} aria-hidden="true"/></Link></div></div>; }
