'use client';
import Link from 'next/link';
import { useState } from 'react';
import { ArrowUpRight } from 'lucide-react';
import { newsletterSchema } from '@/content/forms';
export function Newsletter(){const[status,setStatus]=useState<'idle'|'sending'|'success'|'error'>('idle');const[error,setError]=useState('');
async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);const parsed=newsletterSchema.safeParse({email:data.get('newsletter-email'),privacy:data.get('newsletter-privacy')==='on'});if(!parsed.success){setError(parsed.error.issues[0].message);setStatus('error');return;}setStatus('sending');setError('');try{const res=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(parsed.data)});if(!res.ok)throw new Error();setStatus('success');}catch{setError('No se pudo completar la prueba. Inténtalo de nuevo.');setStatus('error');}}
return <form className="newsletter-form" onSubmit={submit} noValidate aria-label="Alertas de seguridad">{status==='success'?<div role="status"><h3>Ya conoces el primer paso.</h3><p>Prueba completada. Esta versión no guarda tu correo ni activa una suscripción.</p><button type="button" className="button button-ghost" onClick={()=>setStatus('idle')}>Volver</button></div>:<><label htmlFor="newsletter-email">Correo electrónico</label><div className="newsletter-input"><input id="newsletter-email" name="newsletter-email" type="email" placeholder="Tu correo de trabajo" autoComplete="email" aria-invalid={status==='error'} aria-describedby="newsletter-feedback" disabled={status==='sending'}/><button aria-label="Solicitar alertas" disabled={status==='sending'}><ArrowUpRight/></button></div><div className="consent"><input id="newsletter-privacy" name="newsletter-privacy" type="checkbox"/><label htmlFor="newsletter-privacy">He leído el <Link href="/privacidad">aviso de privacidad</Link>.</label></div><p id="newsletter-feedback" role="status" className={error?'field-error':'microcopy'}>{error||(status==='sending'?'Validando…':'Formulario de demostración. No activa envíos.')}</p></>}</form>}
